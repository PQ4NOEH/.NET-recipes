namespace Heracles.Web.Security
{
    internal static class SecurityUtilities
    {
        internal static void CheckParameter(ref string parameter, int maxSize, string paramName)
        {
            if (string.IsNullOrWhiteSpace(parameter))
            {
                
            }
        }
    }
}